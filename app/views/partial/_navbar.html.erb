<div class='fixed-top'>
  <div class="d-flex flex-column flex-md-row align-items-center p-3 px-md-4 mb-3 bg-white border-bottom shadow-sm">
      
      <a class="logo my-0 mr-md-auto font-weight-normal" href="/"><font color="#000000">ゴシメイ！</a>
      <nav class="my-2 my-md-0 mr-md-3">

        <a class="btn btn-dark" href= "/posts/new">
       ボシュウする</a>
      <a class="btn btn-dark" href="/contacts/new">お問い合わせ</a>
    <% if user_signed_in? %>
       <%#= link_to "マイページ",user_path(current_user)%>
       <% end %>
      <a class="btn btn-dark" href="/pages/company_home">企業の方はこちら</a>
      
<div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav ml-md-auto align-items-center">
          <% if user_signed_in? %>
            <%#= link_to "", user_path(current_user), class: "nav-link commonNavIcon profile-icon" %>
            <%= link_to "トップページ", root_path, class: "btn btn-primary"%>
           <% end %>
      </ul>
      </div>
      </nav>
    </div>
  </div>   
  </div>

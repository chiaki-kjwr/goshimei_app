<% provide(:title, "エージェントをゴシメイする") %>

<%= search_form_for @search, url: posts_path do |f| %>
<div class="form-group">
  <%= f.label :title_or_contents_cont, 'エージェントを検索しよう' %>
  <%= f.search_field :title_or_contents_cont,class:"form-control"%>
  </div>
  <div class="actions"><%= f.submit '検索' %></div>
<% end %>

  <div class="row">
    <% @posts.each do |post| %>
      <div class="col-md-6">
        <div class="card"%>
          <div class="card-body text-center">
            <h4 class="card-title"><%=post.title %></h4>
            <p class="card-text"><%=post.contents %></p>
            <%= image_tag post.company.profile_photo_url(:thumb),class: "round-img" %>
<%#= image_tag post.company.profile_photo unless post.company.profile_photo..nil? %></p>
            <strong><%= post.company.name %></strong></br>
            <a href="#" class="btn btn-dark">ゴシメイする</a>
          </div>
        </div>
    </div> 
  <%end%>
</div>

